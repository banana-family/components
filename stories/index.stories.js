import React from 'react'

import { storiesOf } from '@storybook/react-native'

import { View } from 'react-native'

storiesOf('Welcome', module).add('to Storybook', () => (
  <View>Welcome to Storybook</View>
))
